"""
install.sh - installs en-linux so anyone can use it with one command

What this script does:
1. Checks Python and git are installed
2. Downloads code from GitHub
3. Installs the Python dependencies into an folder
4. Creates the `en-linux` terminal command

"""

!#/bin/bash